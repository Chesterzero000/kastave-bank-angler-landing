# 项目结构

中文项目名：Kastave 岸钓侦察员落地页

## 目录树

```text
.
├── .env.example
├── .github/
│   └── workflows/
│       └── deploy-pages.yml
├── assets/
├── public/
├── scripts/
├── skills/
│   └── american-bass-angler-copy/
├── src/
├── supabase/
│   └── migrations/
├── index.html
├── package.json
├── vercel.json
└── README.md
```

## 目录说明

- `src/`：前端主代码，包括页面组件、文案、样式、追踪和 Supabase 写入。
- `assets/`：产品图、落地页图、社媒广告图等原始素材。
- `public/`：构建时直接公开的静态资源。
- `supabase/migrations/`：落地页数据表、转化指标和预实验统计 SQL。
- `skills/american-bass-angler-copy/`：针对美国 bass bank anglers 的文案生成 skill。
- `scripts/`：构建辅助脚本。
- `.github/workflows/`：GitHub Pages 部署流程。

## 主要内容

这个仓库是一个营销验证型前端项目，核心是验证 Kastave 在美国岸钓人群中的早期需求。它同时承担三个任务：

1. 展示产品价值主张。
2. 收集邮箱、痛点和预约意向。
3. 通过 Supabase、Meta Pixel、TikTok Pixel、GA、Plausible 等渠道记录转化信号。

## 后续整理建议

- 将 `assets/` 继续拆分为 `assets/product/`、`assets/landing/`、`assets/social/`。
- 将 A/B 测试记录补入 `docs/实验记录.md`。
- 将支付、邮箱、广告像素接入步骤补入 `docs/转化追踪.md`。

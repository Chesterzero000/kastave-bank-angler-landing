# 2026-05-20 Progress

## Status

PayPal integration is connected end to end for the PMF landing page.

## Completed

- Added the live PayPal payment link fallback for the $1 reservation CTA.
- Added the Vercel serverless webhook endpoint at `/api/paypal-webhook`.
- Added PayPal webhook signature verification through PayPal Webhooks v1.
- Added handling for `PAYMENT.CAPTURE.COMPLETED`, `PAYMENT.CAPTURE.DENIED`, `PAYMENT.CAPTURE.REFUNDED`, and `PAYMENT.CAPTURE.REVERSED`.
- Added `$1 USD` amount validation for completed captures.
- Added Supabase purchase event recording with duplicate protection.
- Added PayPal post-payment guidance on the reservation flow and `/thanks` page.
- Added tests for the PayPal webhook core, route behavior, default payment link, and post-payment UX copy.

## Next Required Setup

- Rotate the exposed PayPal client secret in PayPal Developer Dashboard.
- Add `PAYPAL_CLIENT_ID`, `PAYPAL_CLIENT_SECRET`, `PAYPAL_WEBHOOK_ID`, `PAYPAL_ENV`, `SUPABASE_URL`, and `SUPABASE_SERVICE_ROLE_KEY` in Vercel.
- Redeploy after Vercel environment variables are set.
